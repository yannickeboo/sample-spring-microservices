 pipeline {
    agent {
        docker {
            docker {          
         image 'maven:3.5.0'         
     } 
            }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
